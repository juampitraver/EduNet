@model TP3.Core.Data.Challenge.ChallengeData
@if (Model != null && Model.Cables != null && Model.Cables.Any())
{
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th></th>
                <th>@Html.DisplayNameFor(model => model.Cables.FirstOrDefault().Name)</th>
                <th>@Html.DisplayNameFor(model => model.Cables.FirstOrDefault().Order)</th>
            </tr>
        </thead>
        <tbody>
            @{
                @for (int i = 0; i < Model.Cables.Count; i++)
                {
                    <tr>
                        <td>
                            <div class="kt-checkbox-inline">
                                <label class="kt-checkbox">
                                    <input asp-for="Cables[i].IsSelect" onclick="updateCable()">
                                    <span></span>
                                </label>
                            </div>
                        </td>
                        <td>
                            <input type="hidden" asp-for="Cables[i].CableId" />
                            <input type="hidden" asp-for="Cables[i].Name" />
                            <input type="text" asp-for="Cables[i].Name" readonly class="form-control" placeholder="@Html.DisplayNameFor(m => m.Cables.FirstOrDefault().Name)" />
                        </td>
                        <td>
                            <input type="text" asp-for="Cables[i].Order" class="form-control" placeholder="@Html.DisplayNameFor(m => m.Cables.FirstOrDefault().Order)" />
                        </td>
                        

                    </tr>
                }
            }
        </tbody>
    </table>
}
